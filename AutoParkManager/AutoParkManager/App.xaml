<Application x:Class="AutoParkManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="LoginWindow.xaml">

    <Application.Resources>
        <!-- Тёмная тема для всего приложения -->

        <!-- Цвета -->
        <SolidColorBrush x:Key="DarkBackground" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#2D2D3D"/>
        <SolidColorBrush x:Key="SurfaceLight" Color="#3A3A4A"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#6C5CE7"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#00B894"/>
        <SolidColorBrush x:Key="DangerRed" Color="#FF7675"/>
        <SolidColorBrush x:Key="InfoBlue" Color="#0984E3"/>
        <SolidColorBrush x:Key="WarningOrange" Color="#E17055"/>

        <!-- Глобальный стиль для всех окон -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource DarkBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- Глобальный стиль для всех TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Глобальный стиль для всех TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceLight}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="8,0,8,0"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryText}"/>
        </Style>

        <!-- Глобальный стиль для всех PasswordBox -->
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource SurfaceLight}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="8,0,8,0"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryText}"/>
        </Style>

        <!-- Глобальный стиль для всех ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceLight}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource SurfaceLight}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        </Style>

        <!-- Глобальный стиль для всех DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource SurfaceLight}"/>
            <Setter Property="RowBackground" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="RowHeight" Value="35"/>
        </Style>

        <!-- Стиль для заголовков DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource AccentPurple}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- Стиль для кнопок -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Стиль для Border -->
        <Style TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
    </Application.Resources>
</Application>